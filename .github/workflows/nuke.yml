name: Nuke the world

on:
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: 
      group: ubuntu-runners
    name: A job to say hello
    steps:
      - id: foo
        uses: igs-treasure-island/action-utils@main
        with:
            GIT_TOKEN: ${{secrets.GIT_TOKEN}}
            REPOSITORY_NAME: 'TestGit'
            ACTION_NAME: 'nuke.yml'
            SUCCESS: 1
      - name: bar
        shell: bash
        run: |
          RUN_NUMBER=${{steps.foo.outputs.RUN_NUMBER}}
          echo $RUN_NUMBER
          
      
