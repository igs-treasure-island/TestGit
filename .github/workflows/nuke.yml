name: Nuke the world

on:
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: igs-treasure-island/action-build-deploy-gke@main
        with:
          GKE_KEY_FILE: ${{ secrets.WINBET_THAI }}
          GKE_PROJECT: winbet-thai
          GKE_CLUSTER: thai-test-slot-cluster-1
          GKE_LOCATION: asia-southeast1-a
