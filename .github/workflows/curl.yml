name: curl test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: 
      group: ubuntu-runners
    name: A job to say hello
    steps:
      - name: Key File
        shell: bash
        run: |
          curl -u "oldrd6:1qaz2WSX" http://test-st.jlfafafa2.com:8070/api/v2/applications -i

