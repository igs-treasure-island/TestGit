name: Simple Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Key File
        shell: bash
        run: |
          CMD='echo "${{secrets.ANDROID_API_KEY_THAI_B64}}" | base64 -d > key.txt; 
               cat key.txt'
          eval $CMD
